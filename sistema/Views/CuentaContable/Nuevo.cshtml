@{
    ViewData["Title"] = "Nueva Cuenta";
}

@await Html.PartialAsync("_MensajeExito")

<div class="main-card mb-3 card">
    <div class="card-body">
        <div class="text-center">
            <h4><b>Nueva Cuenta contable</b></h4>
        </div>
    </div>
</div>

@model farmamest.Models.CuentaContableViewModel
@await Html.PartialAsync("_CuentaContableForm", Model)

<div class="main-card mb-3 card">
    <div class="card-body">
        <div>
            <button class="btn btn-danger" data-bind="click: cancelarRegistroCuenta">
                Cancelar
            </button>
            <button class="btn btn-primary" data-bind="click:registrarCuenta">
                Guardar Cuenta
            </button>
        </div>
    </div>
</div>

@section Scripts {
    <script type="text/javascript" src="~/ViewModelKo/CuentaContableVM.js"></script>
}