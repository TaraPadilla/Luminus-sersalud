@model sistema.Models.ConsultasViewModel
@Html.HiddenFor(a => a.CitaId)
@Html.HiddenFor(a => a.PacienteId)
@Html.HiddenFor(a => a.ConsultaId)
@Html.HiddenFor(a => a.PrimeraConsulta)
@Html.HiddenFor(a => a.HistoriaId)
@Html.HiddenFor(a => a.ExamenFisicoId)

<div class="main-card mb-3 card">
    <div class="card-body">
        <div class="form-row">
            <div class="col-12">
                <h5><b>CAGE</b></h5>
                <hr>
            </div>
            <div class="col-md-12 table-responsive">
                <table class="table table-hover">
                    <tbody>
                        <tr>
                            <td>
                                <input type="checkbox" />
                                ¿Ha tenido usted alguna vez la impresión de que
                                debería beber menos?
                            </td>
                            <td>
                                <input type="checkbox" />
                                ¿Le ha molestado alguna vez la gente criticándole su forma de beber?
                            </td>
                            <td>
                                <input type="checkbox" />

                            </td>
                            <td>
                                <input type="checkbox" />

                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>